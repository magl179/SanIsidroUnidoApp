import { BeafitulDatePipe } from './beafitul-date.pipe';

describe('BeafitulDatePipe', () => {
  it('create an instance', () => {
    const pipe = new BeafitulDatePipe();
    expect(pipe).toBeTruthy();
  });
});
